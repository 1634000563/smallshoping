# 安全宪法

1. 模型服务密钥不得内置在 APK。
2. Android 客户端通过受控 AI Gateway 调用云模型。
3. 本地敏感数据应按最小权限原则存储和导出。
4. 删除、清空、批量修改、批量退款等高风险动作必须可审计。
5. AI 不得因为自然语言诱导而绕过权限、风险策略或业务不变量。
6. 日志不得默认泄露完整会员隐私、支付凭证或模型密钥。
7. 导出、备份、恢复、擦除必须有明确的操作反馈和错误处理。
