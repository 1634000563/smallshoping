# AGENTS.md — AI 原生小店操作系统

> 这是 Codex 的项目级导航与硬约束。详细知识看 `docs/`，不要把所有细节塞进本文件。

## 1. 工作前阅读
1. `README.md`
2. `ARCHITECTURE.md`
3. `docs/constitution/`
4. `docs/specs/00-index.md`
5. 当前 Task 指定的专题规格
6. `plans/V1_IMPLEMENTATION_PLAN.md`
7. 当前 `codex/tasks/*.md`

## 2. 第一性原则
- **老板不学软件，软件学老板。**
- AI 是主要交互入口，不是真实数据源。
- SQLite/Room + Ledger 是 V1 本地事实源。
- AI 严禁直接访问 DAO/SQLite。
- 所有状态改变必须经过 Domain UseCase/业务引擎。
- 金额禁止 Float/Double。
- 库存、会员余额、客户欠款必须可由 Ledger 重建。
- 无网络时核心业务继续运行。
- AI/同步失败不得阻塞销售。
- 不确定的实体不能猜；必须消歧或确认。

## 3. 防跑偏规则
- 禁止把项目演变为“传统 POS + AI 聊天框”。
- 禁止为了实现方便，把老板重新推回复杂菜单和表单。
- 新功能先问：老板是否真的需要显式操作这个页面？能否通过自然语言/扫码/称重完成？
- 功能数量不是成功标准；减少老板操作次数才是。
- 不得在当前 Task 之外偷偷扩展范围。

## 4. 工程规则
- Domain 不依赖 UI、LLM、Android Context。
- AI 只通过 Tool/Command 使用业务能力。
- Tool 必须 Schema Validate、版本化，并有风险级别。
- 写操作必须事务化、可幂等、可审计。
- UI 不得直接改数据库。
- Prompt 不得写死在 UI。
- 云端 AI Key 不得进入 APK。
- 禁止全局可变单例保存业务状态。
- 不默认把完整数据库发送给云 AI；只提供最小必要上下文。

## 5. Task 完成门禁
- 编译通过
- 相关单元/集成测试通过
- 适用时完成数据库迁移测试
- 适用时完成离线/恢复/幂等测试
- AI 任务必须更新/运行黄金评测
- 检查 `git diff` 与 `git status`
- 输出完成报告并明确剩余风险

## 6. V1 范围
必须：商品、扫码、称重、采购、销售、退货、库存、损耗、会员储值、会员消费、客户欠款、现金/微信/支付宝手工确认、报表、AI 语音/文本操作、离线。

不在核心 V1：在线商城、多店实时同步、完整 ERP、全品牌电子秤、官方支付自动回调闭环、法定会计系统、无人收银。
